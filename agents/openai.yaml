interface:
  display_name: "Fast Hatch Pet"
  short_description: "Fast, scale-locked Codex v2 pet production"
  default_prompt: "Use $hatch-pet-fast to create this Codex v2 pet with shared scale, duplicate-lane gates, and compact QA."
